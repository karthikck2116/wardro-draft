# Deployment

Import the repository into Vercel, set all values from `.env.example`, use the normal Next.js build command, and deploy. Verify the canonical site URL, image optimisation, Shopify API version, serviceability source, support contacts, and checkout redirect in Preview before promotion to Production.
