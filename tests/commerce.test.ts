import {describe,expect,it} from 'vitest'; import {commerce} from '@/lib/commerce/repository'; describe('commerce filters',()=>{it('filters by doors',async()=>expect((await commerce.getCollection('all',{doors:3})).every(p=>p.doorCount===3)).toBe(true));it('sorts prices',async()=>{const p=await commerce.getCollection('all',{sort:'price-low'});expect(p[0].price.amount).toBeLessThanOrEqual(p.at(-1)!.price.amount)})});
