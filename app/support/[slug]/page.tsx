export default async function SupportPage({params}:{params:Promise<{slug:string}>}){const name=(await params).slug.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <div className="shell section"><p className="eyebrow">Support</p><h1>{name}</h1><p>Get clear help on eligibility, documentation and next steps. Contact information appears when the support environment variables are configured.</p><div className="content-grid"><article><h2>What to know</h2><p>Delivery and free third-party partner installation apply only to eligible Bengaluru PIN codes. Warranty runs for five years automatically from the invoice date.</p></article><article><h2>Need assistance?</h2><p>Use WhatsApp, phone, email, or a customer account service request once those channels are configured.</p></article></div></div>}
