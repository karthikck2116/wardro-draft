import {commerce} from '@/lib/commerce/repository'; import {ProductCard} from '@/components/commerce/product-card';
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q||'';const items=q?await commerce.searchProducts(q):[];return <div className="shell section"><h1>Search Wardro</h1><form className="search-form"><input name="q" defaultValue={q} placeholder="Search by style, doors or colour" autoFocus/><button className="button">Search</button></form>{q&&<p>{items.length} results for “{q}”</p>}<div className="product-grid">{items.map(p=><ProductCard product={p} context="search" key={p.id}/>)}</div></div>}
